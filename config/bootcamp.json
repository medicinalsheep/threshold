{
  "format": "threshold-bootcamp-link",
  "version": 2,
  "root": "./training/bootcamp",
  "note": "Training data in repo (JSONL + Modelfiles only). Weights stay local via ollama pull. Optional: config/bootcamp.local.json for ramdisk override."
}